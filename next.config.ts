import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hides the floating Next.js badge in development. It sits over the UI and
  // has nothing to do with the product.
  devIndicators: false,
};

export default nextConfig;